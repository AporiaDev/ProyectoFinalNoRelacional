# MongoDB Connection String
# Obtén esta cadena desde MongoDB Atlas después de crear tu cluster
MONGODB_URI=mongodb+srv://usuario:password@cluster0.xxxxx.mongodb.net/tienda_peliculas?retryWrites=true&w=majority

# JWT Configuration
# Genera una clave secreta segura (mínimo 256 bits)
JWT_SECRET=tu_clave_secreta_muy_segura_y_larga_minimo_256_bits_cambiar_en_produccion
JWT_EXPIRATION=86400000

# Server Configuration
SERVER_PORT=8080

